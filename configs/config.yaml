project:
  name: "AI-Powered Intelligent Breed Recognition System for Indian Cattle and Buffaloes"
  version: "0.1.0"
  phase: "Phase 0 - Project Foundation"

server:
  host: "0.0.0.0"
  port: 8000
  reload: true

pipeline:
  detection_model: "YOLO"
  classification_model: "EfficientNet-B0"
  explainability: "Grad-CAM"

system:
  device_fallback: "cpu"
  allow_cuda: true
